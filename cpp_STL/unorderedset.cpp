// Unordered set -> similar to set but does not store in sorted order
// time complexity is O(1)     worst case is very rare and the tc is O(n)
// all operations except lowerbound and upperbound do not work

#include <bits/stdc++.h>
using namespace std;

int main()
{
    unordered_set<int> us;
    return 0;
}